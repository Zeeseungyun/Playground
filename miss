- 서버모듈이 메시지에 종속적이라 테스트코드를 실행하기가 어려움.
    메시지와 서버모듈을 분리했어야했음.
    Packet Header는 따로 관리했어야 했음.

- UI 리소스 관리, APK/DLC관리가 안됨.
- DB 연결에서 동시성 관리 안됨.
    일단 넘어가고 문제 됐을 때 처리하는 걸로..
- Handler의 Interface가 TSharedPtr기반으로만 동작하게 되어있어 UObject기반 클래스에서의 구현이 불편함.
- 후킹 할 수 있는 장치가 있긴 해야 할거 같음.
    현재 패킷 유형 별로 Notify Request 로만 받게 되어있음.
    디버깅 용으로도 유용 하며 매니저격 클래스에서 주고받은 메시지 내용을 통해 값을 반영하도록 할 수 있음.

- Dedi를 매번 직접 띄워 플레이하는 환경이 불편.
